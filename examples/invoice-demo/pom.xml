<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.6</version>
        <relativePath/>
    </parent>

    <groupId>com.terubase.examples</groupId>
    <artifactId>invoice-demo</artifactId>
    <version>0.1.0-SNAPSHOT</version>

    <name>TeruBase Invoice Demo</name>
    <description>Small Spring Boot app showing TeruBase against real invoice JPA entities.</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <terubase.version>0.1.2</terubase.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.github.abasheger</groupId>
            <artifactId>terubase-spring-boot-starter</artifactId>
            <version>${terubase.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.github.abasheger</groupId>
                <artifactId>terubase-maven-plugin</artifactId>
                <version>${terubase.version}</version>
                <configuration>
                    <entityBasePackage>com.terubase.starter.examples.invoice</entityBasePackage>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
